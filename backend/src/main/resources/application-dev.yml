# 개발 전용 오버레이 (SPRING_PROFILES_ACTIVE=dev). 비밀/접속정보는 포함하지 않음 — .env(env) 유지.
spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.cmms: DEBUG
    org.hibernate.SQL: DEBUG
